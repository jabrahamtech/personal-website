---
title: 'Why production voice AI fails after the demo'
summary: 'Most production voice AI fails not because the model is wrong, but because the state machine, latency budget, and barge-in handling were never designed for real callers.'
draft: true
tags: ['voice-ai', 'production']
readTime: '~ 7 min'
---

> **▸ status: drafting**
>
> This piece is being written. The short version: most production voice AI fails not because the model is wrong, but because the state machine, latency budget, and barge-in handling were never designed for real callers.
